{
  "name": "sauce-demo-playwright-tests",
  "version": "1.0.0",
  "description": "Playwright automation scripts for Sauce Demo Shopify site",
  "scripts": {
    "test": "playwright test",
    "test:search": "playwright test search.spec.ts",
    "test:cart": "playwright test addToCart.spec.ts",
    "test:checkout": "playwright test checkout.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.3.0"
  }
}
